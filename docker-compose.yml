version: '3.8'
services:
  sentinel-gateway:
    build: .
    container_name: nomadik-sentinel-gateway
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
    volumes:
      - ./quarantine:/app/quarantine
