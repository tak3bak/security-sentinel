version: '3.8'

services:
  security-sentinel-api:
    build: .
    container_name: security-sentinel-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
